# Copy to .env and fill in real values
# Used by justfile (set dotenv-load) for build/archive/distribute recipes

BUILD_HOST=your-macos-build-host
BUILD_USER=your-user
BUILD_PASS=your-password
TEAM_ID=YOUR_APPLE_TEAM_ID
BUNDLE_ID=com.example.punchlist
DIST_DIR=/path/to/ipa/distribution
DIST_URL=https://your-distribution-host/ipas
