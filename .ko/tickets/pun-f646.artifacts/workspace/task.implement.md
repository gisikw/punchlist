Punchlist dev shell
  just --list  — available recipes
  just check   — type-check Swift
  just build   — simulator build
Build succeeded.

**Summary:**

**Task 1 — `Punchlist/Views/InputBar.swift`:** Added `.buttonStyle(.plain)` to the clear button (after the `label:` trailing closure). This disables SwiftUI's default accent color tinting on the button, so the existing `.foregroundStyle(.secondary)` renders as neutral grey without any blue/tint influence.

No deviations from the plan.
