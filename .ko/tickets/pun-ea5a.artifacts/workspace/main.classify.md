Punchlist dev shell
  just --list  — available recipes
  just check   — type-check Swift
  just build   — simulator build
```json
{"disposition": "route", "workflow": "task"}
```

**Reasoning:** The ticket describes a specific feature to implement—making collapsed cards in project mode respond to hold gestures with the same visual feedback (green fill) and actions (unblocking blocked cards, clearing resolved cards) as the expanded hold bar. It's framing what needs to be built, not reporting broken behavior or asking for investigation. The expected output is code changes.
