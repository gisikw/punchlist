Punchlist dev shell
  just --list  — available recipes
  just check   — type-check Swift
  just build   — simulator build
Implementation matches the plan exactly. Both tasks completed: predicate updated with the precise logic specified, and all four test cases added. No invariant violations, no scope creep, no safety issues.

```json
{"disposition": "continue"}
```
