Punchlist dev shell
  just --list  — available recipes
  just check   — type-check Swift
  just build   — simulator build
```json
{"disposition": "route", "workflow": "task"}
```

This ticket describes a specific code change to implement: make triage tickets count as actionable tickets when determining whether the agent toggle displays. It's stating what needs to be built, not reporting unexpected behavior or asking for investigation.
