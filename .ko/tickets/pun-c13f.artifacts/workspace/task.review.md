Punchlist dev shell
  just --list  — available recipes
  just check   — type-check Swift
  just build   — simulator build
The implementation is correct and complete. The `hasTriage` branch was moved to the top of the if/else chain in `ItemRow.swift:332–344`, exactly as the plan specified. A done ticket with triage now shows the grey circle; a done ticket without triage still shows the green checkmark.

```json
{"disposition": "continue"}
```
