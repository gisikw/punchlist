Punchlist dev shell
  just --list  — available recipes
  just check   — type-check Swift
  just build   — simulator build
Done. Changed `InputBar.swift:24` from `.foregroundStyle(.secondary)` to `.foregroundStyle(.tertiary)`. The type check passed. The x button will now render as a lighter grey using SwiftUI's built-in semantic color hierarchy.
