Punchlist dev shell
  just --list  — available recipes
  just check   — type-check Swift
  just build   — simulator build
```json
{"disposition": "route", "workflow": "task"}
```

This ticket describes a specific UI styling change: make the dismiss button's "x" a lighter grey. It's a straightforward style adjustment with a clear fix in mind, making it a task rather than requiring diagnosis or research.
