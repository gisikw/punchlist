Punchlist dev shell
  just --list  — available recipes
  just check   — type-check Swift
  just build   — simulator build
```json
{"disposition": "route", "workflow": "task"}
```

This ticket describes a specific feature to implement: interactive tap targets on collapsed cards in project view. The requirements are well-defined with clarifying notes already answered. The expected output is code implementing the UI changes (hiding labels, showing SVG action buttons with context-aware behavior).
