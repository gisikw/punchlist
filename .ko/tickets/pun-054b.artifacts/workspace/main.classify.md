Punchlist dev shell
  just --list  — available recipes
  just check   — type-check Swift
  just build   — simulator build
```json
{"disposition": "route", "workflow": "task"}
```

This ticket describes a specific feature to implement: a tap-based UI for the collapsed card's circle in project view, with SVG icons and action buttons (comment, block/unblock, complete). While it mentions that long press "isn't working," the primary focus is on *what to build*, not on investigating why something is broken. The expected output is committed code for this new interaction pattern.
