Punchlist dev shell
  just --list  — available recipes
  just check   — type-check Swift
  just build   — simulator build
```json
{"disposition": "route", "workflow": "task"}
```

This ticket specifies a clear code change: redistribute the circle actions to be evenly spaced rather than bunched. While it frames the issue as a problem (bunching causes mistaps), the solution is explicit and straightforward—adjust the layout/spacing logic. The expected output is committed code that implements the even distribution.
