Punchlist dev shell
  just --list  — available recipes
  just check   — type-check Swift
  just build   — simulator build
```json
{"disposition": "route", "workflow": "task"}
```

This ticket describes specific visual changes to implement: lightening the grey color of the triage circle and ensuring the blue in-progress glow displays correctly over it. It's clearly asking *what to build* (styling updates), not reporting what's broken.
