Punchlist dev shell
  just --list  — available recipes
  just check   — type-check Swift
  just build   — simulator build
```json
{"disposition": "route", "workflow": "task"}
```

This ticket describes a specific feature to implement: adding a clear button (×) to the add ticket input that appears when text exists and clears the input on tap. It's a straightforward UI enhancement with a clear implementation requirement, not a bug report or research question.
