Punchlist dev shell
  just --list  — available recipes
  just check   — type-check Swift
  just build   — simulator build
```json
{"disposition": "route", "workflow": "task"}
```

This ticket describes a specific feature to implement: when triage is selected, auto-focus the input field. It clearly states the desired behavior and has a straightforward fix (wire up auto-focus behavior when triage selection occurs). There's no ambiguity or need for investigation—it's describing what to build, not what's broken.
